package compiler.lexer;

/**
 * Title：词法分析器
 * Description：分析源程序的每一个字符，组合成单词，将单词转化成符号
 * Created by Myth on 4/27/2017.
 */
public class Lexer {
    //TODO 分析SourceCodeList,使用位置，List的哪一行 哪个位置，可以实现转化成符号

    /**
     * 将源代码转换成符号
     * @param startPostion 开始分析的字符位置
     * @return 分析完一个符号后的位置,作为下一次转换的开始位置
     */
    public CharPosition convertToSymbol(CharPosition startPostion) {
        CharPosition endPosition = new CharPosition();


        return endPosition;
    }
}
